---
label: Homepage
hide_body: true
fields:
- name: hero_images
  type: image_gallery
  config: {}
  label: Hero Images
  description: Upload at least 2 images here
- name: hero_title
  type: text
  config:
    required: true
  label: Hero Title
  description: "(title under the first image)"
  default: Ty nejlepší vafle
- name: subtitle
  type: textarea
  default: Waf-Waf to jsou ty nejlepší vafle, palacinky a lívance, které jste meli
    možnost ochutnat.
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
  label: Subtitle
  description: "(content after hero section)"
- name: blogs
  type: blocks
  label: Blogs
  template_types:
  - post
  config:
    min: 
    max: 
  hidden: true
- name: homepage_images_showcase
  type: field_group_list
  fields:
  - name: first_showcase_image
    type: file
    config:
      maxSize: 64
    label: First showcase image
  config:
    min: 
    max: 
    labelField: 
  label: Homepage Images Showcase
  description: Add showcase images on the homepage
- name: breakfast_title
  type: text
  config:
    required: true
  label: Breakfast Title
  default: Snídaňová nabídka Každý všední den od 08:00 – 11:00
- name: breakfast_image
  type: file
  config:
    maxSize: 64
  label: Breakfast Image
  default: "/images/breakfast.png"
- name: delivery_title
  type: text
  config:
    required: true
  label: Delivery Title
  default: Rozvážíme každý den od 08:00 do 21:00

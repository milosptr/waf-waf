---
label: Homepage
hide_body: true
fields:
- name: hero_images
  type: file
  config:
    maxSize: 64
  label: Hero images
  description: "(upload at least 2 images for the hero title)"
- name: hero_title
  type: text
  config:
    required: true
  label: Hero Title
  description: "(title under the first image)"
- name: subtitle
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
  label: Subtitle
  description: "(content after hero section)"
- name: blogs
  type: blocks
  label: Blogs
  template_types:
  - post
  config:
    min: 
    max: 
  hidden: true
- name: homepage_images_showcase
  type: field_group_list
  fields:
  - name: first_showcase_image
    type: file
    config:
      maxSize: 64
    label: First showcase image
  config:
    min: 
    max: 
    labelField: 
  label: Homepage Images Showcase
  description: Add showcase images on the homepage
